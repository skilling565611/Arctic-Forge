# Arctic Forge

Arctic Forge is a lightweight Python tool designed to run standalone now and integrate into a larger Master Application system later.

## Current Features

- Standard-library-only Python startup flow
- JSON settings file using the custom `.Settings` extension
- Startup logging to the `Logs` folder
- Core engine placeholder for future modules

## Run

```powershell
python ArcticForge\App\Main.py
```
